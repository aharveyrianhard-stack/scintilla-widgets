# scintilla-widgets
SCINTILLA WIDGETS — standalone individually-windowable widget routes (/ticker /geiger /youtube /chart + scene-index). Static surface, DB reads only via pg(). Vercel deploys from main.
