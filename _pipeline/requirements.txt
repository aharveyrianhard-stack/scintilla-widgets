# Live extraction only. The unit suite runs with none of these installed:
#   python3 -m youtube_transcripts --self-test
yt-dlp>=2024.1.1          # caption ingestion (no audio/video streams requested)
boto3>=1.34.0             # Cloudflare R2 via the S3 API
psycopg2-binary>=2.9.9    # isolated staging writes
