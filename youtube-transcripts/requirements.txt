youtube-transcript-api>=1.2.4,<2
# Fallback for server/datacenter IPs that YouTube bot-checks (see README):
# yt-dlp fetches the same captions, presenting a proof-of-origin token from a
# bgutil provider running alongside (docker: brainicism/bgutil-ytdlp-pot-provider).
yt-dlp>=2025.5.22
bgutil-ytdlp-pot-provider>=1.0
